# Task 08 — Delete Stale Results

## Status

- [ ] Not started
- [ ] In progress
- [ ] Complete
- [ ] Verified

---

## Goal

Delete all stale result files from the previous (degenerate) pipeline run so that `run_validation.py` starts completely fresh. If any checkpoint or result file remains, `run_validation.py --resume` would skip tasks that need to be re-run with the corrected code.

---

## Files / Directories to Delete

All content inside `machine-learning/experiments/feature-reduction-validation/results/`:

```
results/checkpoints/task_01_complete.json
results/checkpoints/task_02_complete.json
results/checkpoints/task_03_complete.json
results/checkpoints/task_04_complete.json
results/checkpoints/task_05_complete.json

results/cv_fold_results/cv_summary.json
results/cv_fold_results/full_25_folds.csv
results/cv_fold_results/reduced_A_folds.csv
results/cv_fold_results/reduced_B_folds.csv
results/cv_fold_results/reduced_C_folds.csv

results/tuned_params/full_25_params.json
results/tuned_params/reduced_A_params.json
results/tuned_params/reduced_B_params.json
results/tuned_params/reduced_C_params.json
results/tuned_params/search_summary.csv

results/splits/train.pkl
results/splits/val.pkl
results/splits/test.pkl
results/splits/split_manifest.json

results/validated_feature_reduction_config.json
results/validation_report.txt
```

The `results/` directory itself and its subdirectory structure should be preserved (empty directories are fine).

---

## What Must NOT Be Deleted

- The Python source files (`.py`) in `feature-reduction-validation/`
- Anything in `feature-reduction/` (separate experiment)
- Anything in `src/models/models_high_risk_v3/` or `v4/`
- The data files in `data/processed/`

---

## Note on `splits/train.pkl`

`train.pkl` was used by `derive_feature_sets.py` (Task 05). By this point, Task 06 is already complete and the derived feature sets have been approved. It is safe to delete `train.pkl` here — it will be regenerated by Task 01 of the fresh pipeline run.

---

## Verification

After deletion, confirm:
- `results/checkpoints/` directory is empty
- `results/cv_fold_results/` directory is empty
- `results/tuned_params/` directory is empty
- `results/splits/` directory is empty
- `results/validated_feature_reduction_config.json` does not exist
- `results/validation_report.txt` does not exist

---

## Dependencies

- Tasks 01–07 must all be complete before deleting stale results

## Blocks

- Task 09 (pipeline run)
